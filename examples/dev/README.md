# Development Examples

This folder is for one-off local experiments and reference-matching drafts.

Files in this folder are intentionally ignored by Git except for this README. Stable command examples should live directly under `examples/` and be covered by `scripts/validate_project.py`.
